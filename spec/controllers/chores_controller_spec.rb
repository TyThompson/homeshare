require 'rails_helper'

RSpec.describe ChoresController, type: :controller do

  # it "lets users create chores" do
  #   user = create :user
  #   sign_in user
  #   # home = create :home
  #
  #   old_count = user.chores.count
  #   response = post :create, chore: {name: "chore_name"}#,  user_id: user.id, home_id: home.id
  #
  #   expect(response.status).to eq 302
  #   expect(chores.count).to eq old_count + 1
  #   expect(user.chores.last.name).to eq 'chore_name'
  #
  # end


end
